# Common dependencies for the Pistachio VAD adapters.
# Install each upstream method repository's own requirements first when provided.

numpy>=1.23
torch>=2.0
torchvision>=0.15
scikit-learn>=1.2
tqdm>=4.64
matplotlib>=3.5
pandas>=1.5
scipy>=1.9
ftfy>=6.1
regex>=2022.10.31

opencv-python>=4.5
Pillow>=9.0
yacs>=0.1.8
tabulate>=0.9

# Optional: only needed by original training scripts that use Visdom visualization.
visdom>=0.2.4
